# install dependencies
$ pnpm install

# setting env
$ edit env.example
$ edit adjust like your local
$ change name .env

# running migration from prisma
$ npx prisma migrate dev

# running app
$ pnpm start
or
$ npm start